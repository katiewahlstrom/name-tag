Why are ID selectors a bad idea? 
    ID selectors can only be used once, so they can be a bad idea to use. 
What selectors should you prefer when working with CSS?
    Class selectors would be a better idea as they can be used multiple times.  